{
  "name": "privoice-o0-convex-spike",
  "version": "0.0.0",
  "private": true,
  "description": "Throwaway O0 spike: de-risk Flutter <-> Convex (functions, HTTP action, auth, file storage). Not part of the shipping app.",
  "scripts": {
    "dev": "convex dev",
    "deploy:once": "convex dev --once"
  },
  "dependencies": {
    "convex": "^1.42.1"
  }
}
