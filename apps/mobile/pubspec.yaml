name: mobile
description: "Privoice — privacy-first meeting transcription."
publish_to: 'none'
resolution: workspace
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  privoice_core:
    path: ../../packages/core
  privoice_audio:
    path: ../../packages/audio
  privoice_stt:
    path: ../../packages/stt
  privoice_ai:
    path: ../../packages/ai
  privoice_models:
    path: ../../packages/models
  shared_preferences: ^2.3.0
  path: ^1.9.0
  path_provider: ^2.1.0
  flutter_markdown: ^0.7.4
  share_plus: ^10.1.2
  wakelock_plus: ^1.2.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
