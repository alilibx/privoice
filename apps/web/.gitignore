node_modules/
# convex/_generated/ is intentionally committed so `npm test` runs on a fresh
# clone without a Convex deploy (convex-test imports it). Regenerated by
# `convex dev`/`convex codegen` when functions/schema change.
.env.local
dist/
*.tsbuildinfo
bun.lock
