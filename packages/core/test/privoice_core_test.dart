import 'package:flutter_test/flutter_test.dart';

void main() {
  // Placeholder so `melos run test` runs cleanly across every package.
  // Real core tests (storage/models) land with the S2 slice.
  test('core package smoke test', () {
    expect(1 + 1, 2);
  });
}
